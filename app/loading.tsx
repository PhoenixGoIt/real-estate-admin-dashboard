import { Loading } from '@/components/shared/local/Loading'
import React from 'react'

const loading = () => {
  return (
    <section>
        <Loading />
    </section>
  )
}

export default loading